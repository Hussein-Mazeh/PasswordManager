# UI Placeholder

This directory is reserved for future user interface work (CLI enhancements,
desktop app, web UI, etc.). No implementation lives here yet.

When UI layers are introduced, keep supporting assets (templates, static files,
build scripts) scoped under this folder.
